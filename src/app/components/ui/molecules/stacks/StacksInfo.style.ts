import styled from "styled-components";

const StackInfoWrapper = styled.div`
  display: flex;
  justify-content: center;
  flex-wrap: wrap;
  height: 650px;
  overflow-y: scroll;
`;

export default StackInfoWrapper;
